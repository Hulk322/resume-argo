apiVersion: v1
kind: Service
metadata:
  name: resume
  namespace: test
spec:
  selector:
    app.kubernetes.io/name: resume
  ports:
    - protocol: LoadBalancer
      port: 9028
      targetPort: 9028
